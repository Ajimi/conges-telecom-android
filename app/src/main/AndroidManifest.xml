<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.telecom.conges">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- This permissions are neccessary for broadcast receiver -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.request.RequestValidationActivity"></activity>
        <activity android:name=".ui.account.DemandsActivity" />
        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.main.MainActivity" />
        <activity android:name=".ui.request.history.HistoriesActivity" />
        <activity android:name=".ui.request.RequestActivity" />
        <activity android:name=".ui.request.detail.RequestDetailActivity" />
        <activity android:name=".ui.request.SearchFilterProduct" />
        <activity android:name=".ui.ferie.FerieCreateActivity" />
        <activity android:name=".ui.ferie.FerieDisplayActivity" />
        <activity android:name=".ui.calendar.CalendarActivity" />
        <activity android:name=".ui.request.history.SupervisorRequestsActivity" />
    </application>

</manifest>